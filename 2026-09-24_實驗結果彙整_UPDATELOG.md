# 2026-09-24 實驗結果彙整

四組實驗的變因、結論與數據。所有 loss 均為 token 加權平均；`total = main + 0.3 × sub_talker`。

> **資料來源**：`BaseLora_*`、`Select_*`、`Freeze_*`、`loss_history.csv`、`loss_history_30`、`loss_history_60`、`emotion_probe_metrics.csv`。
> **缺漏**：`LoraRank_exp` 底下 `happy60_r2_q_proj`、`happy60_r4_q_proj`、`happy60_r4_q_proj_v_proj` 三輪的 CSV 未取得，本文僅涵蓋同目錄下的 `happy_60_100ep_noES`。

---

## 一、Train_data_exp：資料量

### 變因與變數

| | |
|---|---|
| **操縱變因** | 訓練樣本數（標示為 10 / 30 / 60）|
| **控制變數** | 單一情緒（Happy）、`--emotion_lr 0`、60 epoch、lr 2e-5、rank 8、`q_proj,v_proj` |
| **應變變數** | 最佳 val_total、最終 gap、final_test |

只取 Happy 是為了**把情緒這個變因完全移除**——單一情緒的情緒向量是常數，與 speaker 偏移無法區分；搭配 `--emotion_lr 0`，情緒表全程維持零，等同 speaker-only。

### 數據

| run | 標示樣本數 | epoch | **steps/epoch** | 最佳 val_total | @ep | 末 gap (main) | final_test total |
|---|---|---|---|---|---|---|---|
| train_10 | 10 | 60 | **5** | **2.3828** | 50 | +0.2814 | **2.4128** |
| train_30 | 30 | 60 | **4** | 2.4600 | 38 | +0.4833 | 2.5260 |
| train_60 | 60 | 60 | **8** | 2.4274 | 28 | +0.5736 | 2.4750 |

### 這組數據目前不可用（必須先解決）

**steps/epoch 對樣本數不是單調的：10 樣本跑 5 steps，30 樣本卻只跑 4 steps。** 在固定 batch size 與 `gradient_accumulation_steps=4` 之下這不可能發生——樣本多的那組步數只會更多。

推得 batch size 在各輪之間不一致，或檔名標示的樣本數與實際不符。可用 `ceil(n / (bs × 4))` 反推：60 樣本 8 steps 與 30 樣本 4 steps 都對應 bs=2；但 5 steps 需要 n 落在 33–40（bs=2）或 17–20（bs=1），**兩者都不是 10**。

因此 train_10 呈現的「最佳 val_total 與 final_test 都最好」**不能解讀為「資料越少越好」**——三輪很可能不是同一個設定。

**需要**：三輪的實際樣本數與 batch size。在此之前，資料量這一軸沒有結論。

### 仍然成立的部分

三輪的最終 gap 都是正的且持續擴大（+0.28 / +0.48 / +0.57），驗證曲線都在最佳點之後轉向上。**無論樣本數怎麼算，過擬合在每一輪都發生了。**

---

## 一之二、LoraRank_exp：可訓練參數量

### 變因與變數

| | |
|---|---|
| **操縱變因** | LoRA rank（8 → 4 → 2）與 target modules（`q_proj,v_proj` → `q_proj`）|
| **控制變數** | **完全匹配**：60 樣本 Happy、100 epoch、**每輪皆 8 steps/epoch、800 total steps**、`--emotion_lr 0`、lr 2e-5 |
| **應變變數** | 最佳 val_main、最佳 epoch、最終 gap、final_test |

本組是四批實驗中**對照最嚴謹的一組**：四輪的 steps/epoch 與總步數完全相同，唯一的變因是可訓練參數量。

### 想知道什麼

過擬合能不能靠減少可訓練參數量壓下來。

### 數據

| run | 相對容量 | 最佳 val_main | @ep | 末 train_main | 末 val_main | **末 gap** | final_test total |
|---|---|---|---|---|---|---|---|
| r8 `q,v` | 1× | 1.0126 | 25 | 0.1370 | 1.6088 | **+1.4718** | 2.4367 |
| r4 `q,v` | 0.5× | 1.0123 | 27 | 0.1780 | 1.5696 | **+1.3916** | 2.4413 |
| r4 `q` | 0.25× | 1.0305 | 53 | 0.5938 | 1.1033 | **+0.5095** | 2.4893 |
| r2 `q` | 0.125× | 1.0290 | 60 | 0.6458 | 1.0742 | **+0.4283** | 2.4920 |

### 結論

1. **減少容量有效地壓抑了過擬合。** 末 gap 從 +1.4718 降到 +0.4283（**−71%**），最佳 epoch 從 25 推遲到 60，末 train_main 從 0.1370（記憶）回升到 0.6458（不再背得起來）。
2. **但可達到的驗證 loss 完全沒有改善。** 最佳 val_main 在 8 倍的容量跨度下是 1.0126 / 1.0123 / 1.0305 / 1.0290 —— **全距僅 0.018（1.8%）**，而且低容量那兩輪還略差。final_test 甚至隨容量下降而**變差**（2.4367 → 2.4920）。
3. **因此這不是容量問題。** 容量只決定「多快開始過擬合」，不決定「最好能到哪裡」。天花板由別的東西設定——與六情緒實驗指向同一個結論：文字已經解釋掉大部分可解釋的變異。
4. **真正的槓桿是 target modules，不是 rank。** r8→r4 在 `q,v` 之下幾乎沒有差別（gap 1.4718 → 1.3916）；但 `q,v`→`q` 讓 gap 直接砍半（1.3916 → 0.5095）。**減少「要 adapt 哪些矩陣」比減少 rank 有效得多。**
5. 事前預測（最佳 epoch 往後推、gap 縮小、但驗證曲線仍會轉向上）**四項全部成立**；r2 在 ep60 觸底後仍轉向上，故「低容量就不過擬合」的反例未出現。
6. 四輪的兩個 delta 全程為 0.0000，確認 `--emotion_lr 0` 生效。

### 尚未測試

`--lora_target_modules` 以模組名稱字尾比對，**無法限制「只調前幾層」**。既然 target modules 才是主要槓桿，下一個值得試的是「只在最上面 N 層放 LoRA」，需改 `apply_lora()`。

---

## 二、Lora_all_emotion：六情緒基準線

### 變因與變數

| | |
|---|---|
| **操縱變因** | 無（基準線）|
| **控制變數** | JVNV F2 六情緒（train 296 / val 38）、rank 8、`q_proj,v_proj`、lr 2e-5、emotion_lr 1e-3 |
| **應變變數** | train/val loss、各情緒 loss |

### 想知道什麼

加入情緒向量之後的整體表現，以及過擬合的型態。

### 數據

- 50 epoch，**19 steps/epoch**（batch_size 4）
- 最佳 val_total **2.5245 (ep16)**、最佳 val_main **1.1364 (ep16)**
- 末 epoch：train_main 0.7193、val_main 1.2915、gap **+0.5723**
- final_test：main 1.1289、total 2.5054
- ep17 之後 34 個 epoch 中，**28 個**比前一個更差

### 結論

1. 微調帶來的驗證收益很小：val_main 從 ep1 的 1.2673 只降到 1.1364（**−10.3%**），ep16 之後開始惡化。
2. 最佳點的 train/val gap 僅 +0.145，**不是災難性的記憶**；問題是**驗證從 ep13 起就停住了**，不是訓練跑掉。
3. **本輪不是嚴格對照組**：batch_size 為 4（19 steps/epoch），而 Select 與 Freeze 為 2（37 steps/epoch），且多跑 20 個 epoch。

---

## 三、Lora_select_delta：加入 ablation 指標

### 變因與變數

| | |
|---|---|
| **操縱變因** | 新增 `val_emotion_delta`（把情緒向量歸零再跑一次 validation）|
| **控制變數** | 同上，batch_size 2、30 epoch |
| **應變變數** | val_loss 與 delta 隨 epoch 的變化 |

### 想知道什麼

`val_loss` 能不能反映情緒條件化的成效。先前的推論是：JVNV 的文字本身就帶著情緒，因此情緒向量死掉與正常的模型 teacher-forcing CE 幾乎相同，`val_loss` 對這件事是盲的。

### 數據

- 30 epoch，37 steps/epoch
- 最佳 val_total **2.5213 (ep13)**；delta 峰值 **0.0344 (ep25)**
- 末 epoch：train_main 0.8069、val_main 1.2134、gap +0.4066
- final_test：main 1.1542、total 2.5259
- **ep13 → ep30**：val_total 2.5213 → 2.5982（**+3.1%，明顯惡化**）；delta 0.0317 → 0.0319（**+0.6%，幾乎沒動**）

### 結論

**`val_loss` 與情緒效果是解耦的。** 過擬合惡化 3.1% 的同時，情緒向量的貢獻完全沒受影響。兩者測的是不同的東西，用 `val_loss` 選 checkpoint 對「情緒條件化」這個目標而言近乎隨機。

delta 序列全程在 0.026–0.034 之間浮動，噪聲約 ±0.003，ep25 的「峰值」相對 ep13 的 0.0317 並無顯著差異 —— **delta 在 ep9 之後就進入平台，不存在明顯峰值。**

### 待確認

目錄名為 `select_delta`，但 final_test（2.5259）較接近 ep13 的 val（2.5213）而非 ep25（2.5586），**實際使用的 `--select_by` 需由該輪的 log 確認**。

---

## 四、Lora_select_delta_freeze_Lora：凍結 LoRA

### 變因與變數

| | |
|---|---|
| **操縱變因** | `--freeze_lora_epochs 5`（前 5 個 epoch adapter 的 `requires_grad=False`）|
| **控制變數** | 與 Lora_select_delta **完全匹配**：同 37 steps/epoch、同 30 epoch、同資料 |
| **應變變數** | delta 隨 epoch 的變化、梯度範數 |

這是本批唯一嚴格匹配的 A/B 對照。

### 想知道什麼

LoRA 與情緒表是同一個 loss 下的兩組參數（AdamW 兩個 param group，lr 2e-5 與 1e-3），理論上在搶同一份誤差訊號。若先凍結 adapter 讓情緒表獨佔梯度，情緒效果會不會變強？

### 數據

**delta 隨 epoch：**

```
凍結中(ep1-5)  0.0565  0.0876  0.1086  0.1240  0.1324   單調上升
──────────────────── LoRA 解凍 ────────────────────
解凍後(ep6-30) 0.0896  0.0878  0.0787 ... 0.0523       單調衰減
```

- 解凍瞬間下跌 **−32.3%**（0.1324 → 0.0896），至 ep30 累計 **−60.5%**
- 峰值 0.1324 是 Select 峰值 0.0344 的 **3.8 倍**
- **梯度範數佐證凍結生效**：ep1–5 為 0.187 / 0.151 / 0.160 / 0.197 / 0.161（僅情緒表在更新）；ep6 跳至 1.045
- 最佳 val_total 2.5248 (ep17)、最佳 val_main 1.1365 (ep14)
- final_test：main 1.2876、total **2.7702**（明顯劣於 Select 的 2.5259 與 all_emotion 的 2.5054）

### 結論

1. **競爭假說被證實。** LoRA 不只是沒幫忙，而是**主動侵蝕**情緒表已經取得的作用 —— 解凍是一個明確的轉折點，之後單調衰減。
2. **但 3.8 倍是高估的。** 存檔時的 cosine 顯示凍結那輪六個向量的平均兩兩相似度達 **0.67**（一般共同訓練為 0.25），代表它們大幅地朝同一個方向移動。凍結期間情緒表是唯一能擬合任何東西的參數，最大的未解釋量是「base model 預設聲音 → 本語料」的偏移，而該偏移對六個情緒完全相同。歸零 delta 會把這個共用成分一併算進去。
3. **final_test 明顯較差（2.7702）**，與 ep5 的 val_total（2.7509）接近，顯示該輪還原的是早期 checkpoint。凍結期的模型整體品質差，delta 佔比高有一部分來自「模型還沒學好、殘差多」。
4. 因應 2.：新增 `val_emotion_shuffle_delta`（`emotion_id → (id+1) % 6`）。該指標為輪換，六個向量的使用次數不變，只改變樣本與向量的配對，因此 loss 變化不能歸因於某向量被過度使用；六個一模一樣的向量會得到很大的歸零 delta 與為零的打亂 delta。**本批實驗跑在該欄位加入之前，故無打亂 delta 的數據。**

---

## 五、VA 指標（emotion_probe_metrics.csv）

### 設定

`Lora_all_emotion` 的 checkpoint，4 句**情緒中性的中文**句子 × 6 情緒 = 24 筆，sampled decoding、seed 41。
**注意：模型以日文微調，本輪為跨語言測試。**

### 數據

| emotion | valence | arousal | va_dist | spk_cos | energy_db | rate |
|---|---|---|---|---|---|---|
| anger | 0.435 | 0.765 | 0.182 | 0.564 | −26.55 | 3.40 |
| disgust | 0.386 | 0.670 | 0.266 | 0.469 | −32.80 | 3.70 |
| fear | 0.362 | 0.705 | 0.186 | 0.426 | −33.74 | 3.39 |
| happy | 0.410 | 0.743 | 0.188 | 0.467 | −28.85 | 3.89 |
| sad | 0.408 | 0.645 | 0.205 | 0.506 | −32.91 | 3.12 |
| surprise | 0.375 | **0.796** | 0.199 | 0.515 | −31.27 | 3.54 |

**效果量（情緒造成的變異 ÷ 句子造成的變異）：**

| 指標 | 效果量 |
|---|---|
| energy_db | **1.16** |
| **arousal** | **0.59** |
| chars_per_sec | 0.20 |
| f0_median | 0.18 |
| **valence** | **0.04** |

### 結論：不是整個指標壞掉，是 valence 那一軸壞掉

1. **arousal 是正常的。** 排序為 `surprise > anger > happy > fear > disgust > sad` —— 高激發（驚訝、憤怒、開心）在上，悲傷在最下，**完全符合心理學預期**。效果量 0.59。
2. **valence 是平的。** 六個值全部落在 0.362–0.435（全距 0.073），效果量 **0.04** —— 情緒只解釋了句子變異的 4%。**在這種情況下讀出來的任何排序都是雜訊排序，不具意義。**
3. **「Anger 和 Happy 分不開」在 arousal 上是正確行為。** 兩者同為高激發（0.765 vs 0.743），本來就該靠 valence 區分；valence 既然是平的，它們當然分不開。這是 valence 的失效，不是 arousal 的。
4. 兩個可能原因指向同一方向，且會疊加：
   - **跨語言 SER 的已知特性**：arousal 靠聲學（能量、音高、語速）可跨語言轉移；valence 依賴更細緻的線索、且部分模型實際在讀語意內容。
   - **模型本身可能就沒有控制 valence**。先前日文的韻律探針顯示能量（1.92）與語速（1.59）成功、音高相關特徵失敗，而能量與語速正是 arousal 的經典代理，音高輪廓與嗓音品質才承載 valence。**本次 VA 結果與該預測一致。**
5. 跨語言代價明顯：energy 效果量由日文的 1.92 降至 1.16，rate 由 1.59 降至 0.20。
6. `speaker_cosine` 介於 0.426–0.564，偏低，合成音色與參考音檔的相似度值得另外檢查。

### 決定與保留意見

**決定（2026-09-24）：VA 指標停用，另尋替代。** 理由是「一半錯等於全部錯，指標只剩一半可信度」。原始錄音的對照實驗不做了，VA 模型名稱與訓練語料語言亦未記錄（環境已清除）。

**保留意見（記錄供日後判斷）**：「一半錯等於全部錯」在這裡不成立，理由有三——

1. **valence 與 arousal 是兩個獨立的輸出維度**，不是同一個數字的兩半。valence 失效不構成 arousal 失效的證據。
2. **arousal 有獨立的佐證。** 韻律探針（能量效果量 1.92、語速 1.59，日文；能量 1.16，中文）完全不經過 VA 模型，卻與 arousal 的排序一致。**同一個結論由兩個獨立的量測支持**，這比單一指標更強，而不是更弱。
3. **valence 平掉本身可能就是真結果**，與「音高相關特徵失敗」的韻律觀察一致。若如此，它是一個發現，不是儀器故障。

因此更精確的結論是：**可以宣稱 arousal 受到控制，不能宣稱 valence 受到控制。** 停用整個 VA 會一併丟掉目前最強的正面證據。

若日後要重啟，最小的判定實驗仍是：把同一個 VA 模型跑在原始 JVNV F2 錄音上。

| 原始錄音的 VA 結果 | 結論 |
|---|---|
| valence 一樣平／不合理 | 模型或語言不匹配，換模型即可，合成音檔無辜 |
| valence 排序正確 | 合成音檔**真的沒有 valence 資訊**，是真結果 |
| arousal 合理、valence 不合理 | 典型跨語言 SER；arousal 軸可用，valence 軸放棄 |

## 五之二、本文的推論與未確認項

以下**不是從檔案讀到的**，是從數據反推的，需以各輪的執行指令或 stdout 核對：

| 推論 | 依據 | 風險 |
|---|---|---|
| `BaseLora_*` ↔ `Lora_all_emotion`、`Select_*` ↔ `Lora_select_delta`、`Freeze_*` ↔ `..._freeze_Lora` | 欄位有無 delta、steps/epoch、凍結期的梯度範數 | 若對應錯誤，三、四節的歸屬全錯 |
| batch_size：all_emotion = 4、Select/Freeze = 2 | 296 樣本下 19 與 37 steps/epoch 反推（accum 固定為 4）| 影響「非嚴格對照組」這個說法 |
| Freeze 還原的是 ep5 | final_test total 2.7702 接近 ep5 的 val 2.7509 | 中 |
| Select 實際用的 `--select_by` | final_test 2.5259 較接近 ep13 而非 ep25 | 與目錄名 `select_delta` 矛盾，**最需要核對** |
| 「一般共同訓練 cosine 0.25」出自哪一輪 | 先前貼出的 emotion_table.pt 輸出，未標註來源 | 影響四、2. 的對比基準 |

**已確認無法取回**：各輪的執行指令與 cosine 矩陣（環境已清除）、VA 模型名稱。上表各項因此無法再核對，引用四、2. 的「3.8 倍高估」時須註明依據已不可查。

## 六、跨實驗的總結

1. **過擬合不是資料量問題。** 用上 60 句 Happy、跑滿 100 epoch 無早停，train_main 降到 0.1370（記憶）而 val_main 升 58.9%。（資料量那一軸本身因 batch size 不一致而無結論，見一節。）
2. **過擬合也不是容量問題。** 容量降 8 倍，末 gap 減少 71%、最佳 epoch 從 25 推遲到 60，但**最佳 val_main 只動了 1.8%，final_test 反而變差**。容量決定「多快過擬合」，不決定「最好能到哪」。
3. **`val_loss` 無法反映情緒條件化。** Select 那輪 val_total 惡化 3.1% 的同時 delta 只動 0.6%，兩者解耦。
4. **LoRA 會侵蝕情緒表的作用。** 解凍後 delta 單調衰減 60.5%，梯度範數證實凍結確實生效。
5. **情緒向量控制得了 arousal，控制不了 valence。** 韻律探針與 VA 探針從兩個獨立管道得到一致結論。
6. **兩個「不是」指向同一件事**：加資料與減容量都改變不了驗證的天花板，代表限制來自資料本身的性質（文字已隱含情緒），而非模型的規模或訓練量。

## 七、尚未完成

- 資料量那一軸需重跑或補齊實際樣本數與 batch size（見一節）。
- `--emotion_dropout`、`--emotion_scale` 與 `val_emotion_shuffle_delta` 加入後尚未重跑任何六情緒實驗；四、2. 的「3.8 倍高估」尚未用打亂指標重新量測。
- 「只在最上面 N 層放 LoRA」未測試（需改 `apply_lora()`）。既然 target modules 是主要槓桿，這是下一個值得試的方向。
- VA 的替代指標未定。
