# 2026-09-24 情緒表競爭與指標修正 更新紀錄

## 1. 本次目的

9/22 建立的診斷工具顯示情緒向量**有在作用但很弱**（能量效果量 1.92、語速 1.59，排序符合心理學預期）。本日處理兩件事：在不動資料的前提下讓情緒表拿到更大一份梯度，以及修正「情緒向量到底有沒有用」的量測方式。

## 2. 今日提交

| Commit | 時間 | 主旨 |
|---|---|---|
| e9d0e13 | 13:47 | 新增 --freeze_lora_epochs 與 --select_by emotion_delta |
| 7299da9 | 15:37 | 新增 --emotion_dropout、--emotion_scale，以及打亂情緒標籤的 ablation |
| bdf3dbf | 15:46 | 新增 rebake_emotion_scale.py：不重訓就能改情緒強度 |
| c7ecdcd | 16:33 | --emotion_lr 0 時允許單一情緒的資料集 |
| d5946f8 | 16:48 | 每次訓練結束自動輸出 loss 折線圖 |
| 63e61bb | 16:55 | --emotion_lr 0 時 emotion 欄位改為選填 |
| cd98720 | 18:48 | 新增 --center_emotion_table（**已於工作區撤回，見 5.2**）|

工作區尚有三個檔案的修改未提交（撤回 cd98720 與對應的 README 改寫）。

## 3. 核心發現：LoRA 與情緒表在搶同一份誤差訊號

兩者是同一個 loss 下的兩組參數（`sft_12hz_Lora.py` 的 AdamW 有兩個 param group，學習率 2e-5 與 1e-3）。LoRA 看得到文字、容量又大，在文字本身隱含情緒的語料上會先解釋掉大部分，情緒表只拿得到殘差。

`--freeze_lora_epochs 5` 的實驗直接證實了這點：

```
凍結中  0.056 → 0.088 → 0.109 → 0.124 → 0.132   單調上升
─────────────────── LoRA 解凍 ───────────────────
解凍後  0.090   0.088   0.079 ...  0.052        單調衰減（自峰值 −61%）
```

梯度範數佐證凍結確實生效：ep1–5 為 0.15–0.20（只有情緒表在動），ep6 跳到 1.04。

**LoRA 不只是沒幫忙，而是主動把情緒表已經學到的作用侵蝕掉。**

另一個對照（Select 那輪）顯示 `val_loss` 與情緒效果是**解耦**的：ep13 → ep30 之間 val_total 惡化 +3.1%，而 delta 只動了 +0.6%。過擬合惡化完全沒有影響情緒條件化——這證實 val_loss 對情緒條件化是盲的。

## 4. 兩個 ablation 指標

兩者都只取 `main_loss`（sub-talker 全程在平台期，納入只會增加雜訊），每個 epoch 寫進 `loss_history.csv`。

| 欄位 | 把情緒向量換成 | 回答的問題 |
|---|---|---|
| `val_emotion_delta` | 零向量 | 這張表**有沒有貢獻** |
| `val_emotion_shuffle_delta` | `(id + 1) % 6`，即別的情緒的向量 | 模型**分不分得出六種情緒** |

**報告時用打亂那個。** 歸零會同時拿掉「六個彼此不同」和「這張表學到的所有東西」，一個實驗動了兩個變因。

決定性的反例：假設六個向量訓練完變得一模一樣，情緒控制徹底失敗（每個情緒產生相同音檔）。此時歸零 delta **仍然很大**（那個共用向量確實有學到東西），給出漂亮但虛假的數字；打亂 delta 正好是 **0**（換了等於沒換），誠實報告失敗。

凍結那輪的六個向量平均兩兩 cosine 到 0.67（一般共同訓練是 0.25），距離這個退化狀態不遠，所以這不是假想問題。

### 為什麼是 `+1 % 6`

- **輪換（permutation）**：六個向量被用到的**次數完全不變**，只有「樣本配哪個向量」改變。因此 loss 上升不能歸因於某個向量被過度使用。這是「換成平均」做不到的（平均會把六個全換成一個新東西，用量整個改掉）。
- 1 不是 6 的倍數，所以**沒有任何樣本保留自己的向量**，不會被稀釋。
- 確定性，不同 epoch 與不同輪訓練可直接比較。
- 移位量 1 無特殊意義（`EMOTIONS` 照字母排，順序無語意），1..5 任一皆可；嚴謹版是五種輪換取平均，代價是多四次 validation。

## 5. 架構決策

### 5.1 採用

- **`--freeze_lora_epochs N`**：前 N 個 epoch 將 adapter 的 `requires_grad` 設為 False。情緒表 `requires_grad=True`，計算圖完整，backward 仍會穿過凍結的權重抵達情緒表；adapter 只是不累積梯度、被 AdamW 略過。
- **`--emotion_dropout P`**：訓練時以機率 P（逐樣本）把情緒向量歸零。模型必須在「有」與「沒有」之間表現不同，因而無法讓情緒表變得多餘——直接對付 3. 觀察到的衰減。
- **`--emotion_scale α`**：匯出時把情緒偏移乘以 α 寫進 `codec_embedding`。speaker 向量不縮放，音色不變。**前提是訓練時用過 `--emotion_dropout`**，否則模型沒見過該向量變動，外推沒有依據。
- **`rebake_emotion_scale.py`**：訓練把未縮放的 speaker 向量與情緒偏移存進 `emotion_table.pt`，足以重建那六列，所以換 α 只是改檔案而非重訓。從 checkpoint 自己的 `config.json` 讀「名稱 → 列號」，不猜 slot 編號。
- **自動 loss 折線圖**（`plot_loss_history.py`）：訓練結束輸出 `loss_curve.png`。畫圖失敗只印訊息不中斷。兩條線同時用顏色與虛實線區分（配色跑過 CVD 檢查，最差相鄰對 ΔE 24.7）。

### 5.2 放棄：`--center_emotion_table`（零均值約束）

曾於 cd98720 實作：查表時扣掉六列平均，強制六個向量總和為零，使共用成分無處可住、只能回到 LoRA。數值測試確認有效（模擬「六向量同向漂移」時平均 cosine 0.993 → −0.194，梯度正確地變成乘上 `I − 1/6·ones`）。

**放棄理由：無法用一句話對外解釋。** 「哪一部分算共用」需要先定義「共用 = 平均」這個前提，而該前提本身需要辯護（見 6.2）。打亂指標在**不需要任何拆解假設**的情況下達成同樣的診斷目的，因此保留打亂、撤回置中。工作區已反向套用。

### 5.3 其他

- `--emotion_lr 0` 時放寬兩處限制，讓情緒欄位出現之前準備的 manifest（如 `frozen_stage2_seed41`）可直接使用：`report_emotion_counts` 不再要求六情緒齊全；`load_jsonl` 的 `emotion` 改為選填。
- 缺欄位的樣本 `emotion_id = -1`，而 `nn.Embedding` 不接受負索引，故 `compute_loss` 改為查 row 0 再乘上 `(id >= 0)` 遮罩。遮罩同時擋住梯度，row 0 不會因 clamp 收到不該有的更新（已數值驗證）。

## 6. 概念釐清

### 6.1 delta 是什麼（使用者複述後確認）

同一個模型、同一批資料、同一個 CE 函式，**只換一個輸入值**：

```
正常  input_codec_embedding[:, 6, :] = speaker_embedding + emotion_embedding
歸零  input_codec_embedding[:, 6, :] = speaker_embedding + 0
delta = loss(歸零) − loss(正常)
```

重點釐清：**不是「模型結構」清零，是「輸入」清零。** `emotion_table` 的權重完全沒被碰過，只是「查了表但不用那個值」。這是輸入層級的 ablation。在 `@torch.no_grad()` 與 `model.eval()` 下執行，不影響訓練。

選零而非其他值的理由：情緒表是零初始化的，`speaker + 0` 恰為訓練起點，也等於 speaker-only 微調的狀態。

使用者對打亂版本的複述：「**就是故意給錯誤資訊。這樣才知道有沒有用到資訊**」——正確。補上的精確性：因為是輪換，六個向量的用量不變，所以 loss 變化可歸因於配對而非用量。

### 6.2 「共用成分」的說法已撤回

曾主張「delta 被共用成分灌水」，並用平均定義共用部分。使用者兩度質疑：

1. 「數字上就是相減而已，大家都有共同不就沒差了」——**部分正確**。共用成分只在「兩次比較都有它」時才會抵消；歸零是把它整個拿掉，所以不會抵消。但這個性質正是打亂版本所具備的。
2. 「怎麼知道 4 是 2+2 還是 3+1？這只是離均差」——**正確的質疑**。「共用部分 = 平均」是一個**選擇的定義**，不是從資料中發現的事實。而且 `loss` 是非線性的，相減**不等於**代數上分解出某個分量；它只是控制變因下的比較。

最終結論：不使用任何分解式的說法。打亂指標不需要定義共用成分即可回答「六個向量分不分得開」。

### 6.3 先前判斷的更正

- 曾斷言情緒向量「收不到梯度、會停在 0」——**錯誤**。實測 norm 1.18–1.30（約 speaker 的 12%），cosine 0.09–0.37。正確說法是文字吃掉**大部分**學習訊號，情緒表拿到殘差，因此是**被削弱**而非死亡。
- 曾建議 JTES 作為平行語料替代——**錯誤**。JTES 為「每情緒各 50 句」，同樣是非平行。
- 曾主張「靜態向量做不出音高輪廓，是架構決定的」——**講得過死**。prefix-tuning 證明靜態前綴可誘發時變行為。且日文有音高重音、中文有聲調，F0 由詞彙層面決定，「F0 呈現句子主導」很可能是語言學事實而非條件化失敗。

## 7. 早停的釐清

老師認為先前實驗「Transformer 還在學就被停下來」。查證：`sft_12hz_Lora.py` 的早停／`ReduceLROnPlateau` 出現次數為 **0**，`git log -S "early_stopping"` 亦無紀錄——**該腳本從未有過早停機制**。有早停的是 `sft_12hz.py`（patience 8）。

誤解來源可能是這行日誌：

```
Restoring best model from epoch 16 with Validation Loss ... before final test.
```

它指的是最後保留哪一組權重，訓練本身跑滿了所有 epoch。

老師「還在學」的部分是對的：BaseLora 跑滿 50 epoch，train_main 0.9917 → 0.7193（−27.5%，持續下降），但 val_main 1.1364 → 1.2915（+13.7%），ep17 之後 34 個 epoch 有 28 個比前一個更差。**「還在學」與「過擬合」不互斥**——訓練 loss 下降而驗證 loss 同時上升就是過擬合的定義。

Happy-only、100 epoch、無早停的驗證：最佳驗證在 ep29（total 2.3630），ep100 為 2.9990；main loss 的訓練值降到約 0.1（等同把 60 句背起來），驗證值從約 1.01 升到約 1.60（+58%）。

## 8. 待驗證的問題

1. **VA 指標失效**——目前兩個 delta 都建立在 teacher-forcing CE 上，量的是「模型預測是否依賴情緒向量」，**完全沒有碰到生成出來的音檔**。VA 本應補上這一層。需確認失效型態：跑不起來（環境）、輸出無法分離六情緒（可能是真結果而非 bug）、或數值明顯不合理。三者處理方式不同。退路是擴充 `check_emotion_synthesis.py` 的韻律特徵——能量與語速已是 arousal 的代理，可補音高變異度與頻譜傾斜；valence 較難，確實需要模型。
2. `frozen_stage2_seed41/Happy/train_60.jsonl` 是否已是全部的 Happy 資料（待查 `manifest_summary.md`）。若是，「用上全部資料仍過擬合」這一步已完成。
3. 容量階梯（rank 8→4→2、target modules `q_proj,v_proj`→`q_proj`）是否能壓下過擬合。預期：最佳 epoch 往後推、gap 縮小，但驗證曲線仍會轉向上。若 rank 2 真的不過擬合，則該預期被推翻。
4. `--lora_target_modules` 以模組名稱字尾比對，**無法限制「只調前幾層」**。若整個階梯跑完過擬合仍在，下一步需改 `apply_lora()`。
5. `--emotion_dropout 0.1` 是否能壓住 3. 觀察到的衰減曲線。
6. 三輪對照的 batch size 不一致：BaseLora 為 19 steps/epoch（bs=4），Select 與 Freeze 為 37（bs=2）。**BaseLora 非嚴格對照組**；Select vs Freeze 匹配，主結論不受影響，但報告需說明。
